// This layout is no longer needed as the parent (dashboard) layout handles DashboardLayout
export default function RepositoriesPageLayout({ children }: { children: React.ReactNode }) {
    return children;
}
